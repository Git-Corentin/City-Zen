<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>

    <!-- Lien vers Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Amarante&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Amarante&family=Tenor+Sans&display=swap" rel="stylesheet">
    <style>
        /* Réinitialisation des marges et padding sur html, body */
        html, body {
            margin: 0;
            padding: 0;
            height: 100%; /* Utiliser toute la hauteur de l'écran */
            overflow-x: hidden; /* Désactiver le défilement horizontal */
        }

        /* Espacement du body pour ne pas être recouvert par la navbar fixe */
        body {
            padding-top: 80px; /* Ajuster selon la hauteur de la navbar */
            display: flex;
            flex-direction: column;
            font-family: "Tenor Sans", sans-serif;

            min-height: 100vh;

        }
        .amarante {
            font-family: "Amarante", serif;
            font-weight: 400;
            font-style: normal;
            color: #010535;
            font-size: 2em;
        }
        .tenor-sans-regular {
            font-family: "Tenor Sans", sans-serif;
            font-weight: 400;
            font-style: normal;
            color: #010535;
            font-size: 1.5em;
        }


        /* Styles pour la barre de navigation */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 2000; /* Assurer que la navbar est au-dessus du contenu */
            background-color: #333;
            color: white;
            padding: 10px 0;
        }

        /* Ajouter des styles pour les liens de la navar */
        .nav-link {
            transition: background-color 0.3s, color 0.3s;
            font-weight: bold;
        }

        .nav-link:hover {
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .btn-outline-light {
            border: 1px solid white;
            color: white;
        }

        .btn-outline-light:hover {
            background-color: white;
            color: #28a745;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }

        /* Ajoutez un peu d'espace au contenu principal */
        .content {
            padding: 20px;
        }
    </style>

    {% block stylesheets %}
    {% endblock %}
</head>
<body>

<!-- Barre de navigation -->
<nav class="navbar navbar-expand-lg" style="background-color: #010535 ;">
    <div class="container-fluid">
        <a href="/">
            <img src="{{ asset('homepage/logo_colonne_jaune.png') }}" alt="colonne" style="height: 48px;">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="#" style="color: #dada4e;" >Destination</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" style="color: #dada4e;">Contact</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="/logout" style="color: #dada4e;">Déconnexion</a>
                    {% else %}
                        <a class="nav-link" href="/login" class="btn btn-outline-light me-2" style="color: #dada4e;">Se connecter</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary rounded-pill text-light" href="/kind/offer">Nos offres</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- Contenu principal -->
<main class="content">
    {% block body %}{% endblock %}
</main>

{% block javascripts %}
{% endblock %}

<!-- Scripts Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
